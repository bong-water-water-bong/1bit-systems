{
  "name": "@1bit-systems/cli",
  "version": "0.1.0",
  "description": "Single-binary control plane for 1bit-systems (CLI + OpenAI-compat reverse proxy).",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build --compile --target=bun-linux-x64 --outfile=1bit ./1bit.ts",
    "dev": "bun run ./1bit.ts"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.4.0"
  }
}
