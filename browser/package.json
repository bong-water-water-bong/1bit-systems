{
  "name": "halo-browser",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Bun + puppeteer-core scripts that attach to the halo-browser Chrome profile",
  "scripts": {
    "attach":   "bun run src/attach.ts",
    "screenshot": "bun run src/screenshot.ts",
    "fetch":    "bun run src/fetch.ts"
  },
  "dependencies": {
    "puppeteer-core": "^23.7.1"
  }
}
