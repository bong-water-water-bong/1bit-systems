# Auto-generated by 1bit-caddy-bearers.sh — DO NOT EDIT BY HAND.
# This tracked copy is the fresh-install seed. Real snippet lives at
# /etc/caddy/bearers.conf and is rebuilt whenever /etc/caddy/bearers.txt
# changes (1bit-mesh-invite.sh / 1bit-mesh-revoke.sh call
# strixhalo/bin/1bit-caddy-bearers.sh --reload).
#
# On first install copy this file to /etc/caddy/bearers.conf with
# root:caddy 0640, then add a real bearer via 1bit-mesh-invite.sh.
# The REPLACE_ME placeholder below can never match a real header, so
# every route stays behind its 401 until a real token is provisioned.
(bearer_matcher) {
    @authorized header_regexp Authorization ^Bearer\s+sk-halo-REPLACE_ME$
}
